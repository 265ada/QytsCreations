{
  "version": "1.58",
  "notes": "v1.58: Fix recording freeze when switching lanes. Removed blocking save_groups() call from every captured event (was writing full JSON to disk on every keystroke). Table rows now batch-rendered every 150ms instead of per-event. GUI stays smooth during fast recording."
}
