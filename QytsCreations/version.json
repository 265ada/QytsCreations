{
  "version": "1.60",
  "notes": "v1.60: Fix dual-window PID collision. When lanes share the same window title (two game accounts), each lane now claims a distinct HWND. Primary gets the 1st match, Secondary the 2nd, etc. Duplicate-PID log and wrong-window input injection eliminated."
}
