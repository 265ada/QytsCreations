{
  "version": "1.64",
  "notes": "v1.64: Fix Tesseract TESSDATA_PREFIX pointing to wrong dir (now points to tessdata/ subfolder directly). Fix Secondary/Third lane PID label showing Primary's window — _refresh_pid_label now uses skip_hwnds to resolve each lane's unique window instance."
}
