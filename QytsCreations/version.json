{
  "version": "1.30",
  "notes": "v1.30: Fix auto-update relaunch — app now correctly reopens after update. Versioned exe named QytCroRec_v{version}.exe. Fix AttributeError in restart (save_groups). Pass version tag to download so temp file is named correctly."
}
