{
  "version": "1.48",
  "notes": "v1.48: Auto-update FIXED — batch script had em-dashes that crashed pathlib.write_text(encoding='ascii') with UnicodeEncodeError. Replaced with plain hyphens. Added errors='replace' safety net so any future Unicode slip can't kill the update again."
}
