{
  "version": "1.47",
  "notes": "v1.47: Update Failed dialog now shows the ACTUAL error (download exception, size mismatch, wrong magic, permission denied, etc.) instead of useless 'see console' — PyInstaller windowed apps have no console. Default timeout raised 60s → 600s for slow networks. Specific PermissionError tells user to move exe out of Program Files. Includes manual-install instructions in detailed text."
}
