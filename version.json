{
  "version": "1.23",
  "notes": "v1.23: Pillow + pytesseract + Tesseract-OCR binary bundled into exe via updated spec; build_setup.bat auto-installs deps and downloads Tesseract if missing; auto-configures bundled tesseract path at runtime."
}
