{
  "version": "1.66",
  "notes": "v1.66: Right-click Runs/Executions labels to reset counters (all groups or per-group). Fix detours pipe-break error spam in crash log — game closing while macro runs is expected, now logs to console only not crash file."
}
