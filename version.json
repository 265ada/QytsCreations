{
  "version": "1.34",
  "notes": "v1.34: Auto-updater FIXED — bypasses close prompt during update, force-quits with os._exit so batch can replace exe. New QProgressDialog shows MB / total / percent. Streaming 64 KB chunks for snappy progress. Batch retries move up to 60 times (handles slow Qt teardown). Logs failure to _qyt_update.log if move never succeeds."
}
