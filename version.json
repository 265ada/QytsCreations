{
  "version": "1.63",
  "notes": "v1.63: Fix 'Failed to remove temporary directory' warning on exit. _quit_app now calls os._exit(0) to bypass PyInstaller bootloader cleanup which was failing to delete _MEI* temp dir on Windows (Defender/DLL still mapped) and showing the dialog."
}
