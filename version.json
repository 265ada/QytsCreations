{
  "version": "1.57",
  "notes": "v1.57: Auto-update downloads to %TEMP% instead of the exe's own folder — fixes 'Access is denied' / 'install dir not user-writable' errors on every update attempt."
}
