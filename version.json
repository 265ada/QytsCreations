{
  "version": "1.41",
  "notes": "v1.41: Update no longer hangs at 100%. Backup copy of running exe moved from Python (synchronous, Defender-scanned, multi-second hang) into the batch swap script (runs after we quit). Download worker thread now ALWAYS emits done_sig in try/finally — no silent hang on exception."
}
