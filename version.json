{
  "version": "1.62",
  "notes": "v1.62: Events no longer disappear. Groups of same-type events now expand by default (collapsed=False) so all steps are always visible. Click group header to collapse if needed. Playback highlight auto-expands any collapsed group it lands in."
}
