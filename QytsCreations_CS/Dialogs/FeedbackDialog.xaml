<Window x:Class="QytCroRec.Dialogs.FeedbackDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Send Feedback" Height="420" Width="560"
        Background="{StaticResource BaseBrush}"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="💬 Send Feedback" FontSize="16" FontWeight="Bold"
               Foreground="{StaticResource BlueBrush}" Margin="0,0,0,4"/>

    <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,10"
               Foreground="{StaticResource Subtext0Brush}"
               Text="Describe a bug, suggest a feature, or just say hi. Opens GitHub Issues — you'll need a free GitHub account."/>

    <TextBox x:Name="TxtFeedback" Grid.Row="2"
             AcceptsReturn="True" TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             FontFamily="Consolas" FontSize="12"/>

    <CheckBox x:Name="ChkAttachLog" Grid.Row="3" Margin="0,10,0,0"
              Content="Attach last 50 lines of diag log" IsChecked="True"/>

    <StackPanel Grid.Row="4" Orientation="Horizontal"
                HorizontalAlignment="Right" Margin="0,10,0,0">
      <Button Content="Open Issues Page" Width="140" Margin="0,0,8,0" Click="BtnIssues_Click"/>
      <Button Content="Copy to Clipboard" Width="140" Margin="0,0,8,0" Click="BtnCopy_Click"/>
      <Button Content="Close" Width="80" Click="BtnClose_Click"/>
    </StackPanel>
  </Grid>
</Window>
